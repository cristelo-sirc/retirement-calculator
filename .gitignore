# Internal planning docs stay local; public project docs are explicit exceptions.
*.md
!README.md
!CHANGELOG.md
!CLAUDE.md
!CLAUDE-legacy.md
*.txt

# Personal data files — NEVER commit (repo is public; e.g. saved scenario JSONs)
*.json

# macOS
.DS_Store

# Claude/Cowork session data
.claude/

# Archived / superseded local files (kept locally, never committed)
_archive/
data/
Retirement_Calculator_v*.html

# FUSE mount artifacts
.fuse_hidden*

# Deploy token (private; never commit)
*token*.txt
deploy-token*

# Node tooling (local-only; used for JSX syntax checks, never deployed)
node_modules/

# V19.9 (A7): local scratch harnesses — obsolete absolute paths / stale field assumptions.
_harness.js
_h2.js
